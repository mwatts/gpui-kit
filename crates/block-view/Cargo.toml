[package]
name = "gpui-component-block-view"
description = "Block snapshot painting and BlockLayouts for the block editor"
version = "0.1.0"
edition.workspace = true
license = "MIT"
publish = false

[lib]
name = "gpui_component_block_view"
doctest = false

[dependencies]
block-markdown.workspace = true
gpui.workspace = true
gpui-component.workspace = true
loro = { workspace = true }

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }

[lints]
workspace = true
